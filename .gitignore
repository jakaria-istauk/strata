# OS
.DS_Store

# Backups / archives
*.zip
*.bak
*.original.*

# Dependencies / build (future Tailwind CLI)
node_modules/
dist/
app/assets/*.min.css

# Local
*.log

# Design mockups (reference only, not shipped)
ui/

# Legacy Adminer (reference backup)
legacy/
