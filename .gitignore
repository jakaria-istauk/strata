# OS
.DS_Store

# Backups / archives
*.zip
*.bak
*.original.*

# Dependencies / build
node_modules/
dist/
app/assets/*.min.css

# TypeScript project-references build artifacts (tsc -b)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Local
*.log

# Design mockups (reference only, not shipped)
ui/

# Legacy Adminer (reference backup)
legacy/
